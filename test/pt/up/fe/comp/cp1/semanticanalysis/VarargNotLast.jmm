class VarargNotLast {
    public int invalidMethod(int... nums, int a) {
        return a + (nums.length > 0 ? nums[0] : 0);
    }

    public static void main(String[] args) {
        VarargNotLast obj = new VarargNotLast();
        int result = obj.invalidMethod(new int[]{1, 2, 3}, 5);
    }
}
